//WAP  to Revere the String
#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
char str[20];
printf("Enter the String");
gets(str);
printf("The reverse of String is %s",strrev(str));
return 0;
}
